# First Approach use frequency array of size n.
# traverse the original array and increment frequency.
# then traverse freq array and the index with 0 freq will be missing number
# and the index with value = 2 will be extra 
# T.C - O(n)+O(n), S.C - O(n)

